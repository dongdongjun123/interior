# ==========================================================
# 인테리어 프로젝트 통합 의존성
# 루트에서 한 번만: python -m venv venv && pip install -r requirements.txt
# ==========================================================

# --- Web (backend / frontend) ---
Flask>=3.0
Pillow>=10.0
requests
python-dotenv

# --- model1 / model2: Gemini 평면도·무드 분석 ---
google-genai
ultralytics          # YOLO (backend/app.py의 가구 탐지)
numpy
matplotlib
scikit-learn
umap-learn
tqdm

# --- model3(검색) 예정: mood-search — 지금은 비활성 ---
# 프롬프트→유사 이미지 검색 기능을 붙일 때 아래 주석 해제 (torch 계열은 용량 큼)
# torch
# torchvision
# transformers
# pandas
# deep-translator
